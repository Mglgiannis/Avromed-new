import Typography from '@mui/material/Typography';

export default function SettingsButton({ sx }) {

  return (
    <Typography variant="overline" sx={{ color: '#009688' }}>
          AVROMED HOSPITAL
        </Typography>
  );
}

